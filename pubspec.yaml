name: aamusted_timetable_generator
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  window_manager: ^0.3.4
  flutter_smart_dialog: ^4.9.0+6
  font_awesome_flutter: ^10.4.0
  provider: ^6.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
#  hive_flutter:
#    git:
#      url: https://github.com/Emmanuelfrimpong/hive_flutter.git
  intl: ^0.18.0
  excel:
    git:
      url: https://github.com/BST-Forked-Projects/excel.git
  open_app_file: ^4.0.1
  vs_scrollbar: ^0.2.1
  syncfusion_flutter_xlsio: ^21.2.6
  http: ^1.0.0
  url_launcher: ^6.1.11
  string_similarity: ^2.0.0
  syncfusion_flutter_pdf: ^21.2.6
  pdf:
  google_fonts: ^4.0.5
  file_picker: ^6.1.1
  # file_picker:
  #   git:
  #     url: https://github.com/Emmanuelfrimpong/flutter_file_picker.git

 



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  hive_generator: ^2.0.0
  build_runner: ^2.4.4

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    

#flutter pub run flutter_native_splash:create
#flutter packages pub run build_runner build --delete-conflicting-outputs